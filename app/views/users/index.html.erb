<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
	<% @users.each do |user| %>
		<%= render user %>	<!-- this line is exactly the same as the commented line below-->
		<!-- <%= render 'user', :user => user %> -->
	<% end %>
</ul>

<%= will_paginate %>